# Optional FastAPI service (backend_agent/api) — not required for chat if you only run
# the Node backend + Docker agent container.

# Future: Anthropic / multi-agent toggles for routes you add under /api
# ANTHROPIC_API_KEY=

# LM Studio (only needed if FastAPI routes call local models)
LM_STUDIO_BASE_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=local-model
